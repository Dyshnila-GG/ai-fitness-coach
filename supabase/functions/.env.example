# Server-side secrets for Edge Functions. Never expose to the app.
# Local: copy to supabase/functions/.env (loaded by `npx supabase functions serve`).
# Production: `npx supabase secrets set --env-file supabase/functions/.env`.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=
